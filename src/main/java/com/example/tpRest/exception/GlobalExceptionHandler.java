package com.example.tpRest.exception;

import org.springframework.web.bind.annotation.ControllerAdvice;

@ControllerAdvice
public class GlobalExceptionHandler {
    // GlobalExceptionHandler removed per user's request (simple per-service error handling preferred)
}
